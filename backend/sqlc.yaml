version: "2"
sql:
  - engine: "postgresql"
    queries: "queries"
    schema: "migrations/00001_init.sql"
    gen:
      go:
        package: "gen"
        out: "internal/store/gen"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_empty_slices: true
